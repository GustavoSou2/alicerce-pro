import { ReplaceUndescorePipe } from './replace-undescore-pipe';

describe('ReplaceUndescorePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceUndescorePipe();
    expect(pipe).toBeTruthy();
  });
});
